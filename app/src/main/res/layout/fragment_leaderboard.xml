<?xml version="1.0" encoding="utf-8"?>
<TableLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/leaderboard_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".LeaderboardFragment">

    <!-- TODO: Update blank fragment layout -->

    <TextView
        android:id="@+id/leaderboardHeader"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/text_label_leaderboard"
        android:textAlignment="center"
        android:textAllCaps="true"
        android:textSize="32sp"
        android:textStyle="bold"
        app:drawableRightCompat="@drawable/ic_baseline_info_32" />

    <View
        android:id="@+id/divider_0"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:background="#C1C1C1" />

    <EditText
        android:id="@+id/editText_searchUser"
        android:layout_width="match_parent"
        android:layout_height="54dp"
        android:drawableStart="@drawable/icon_search"
        android:ems="10"
        android:hint="@string/leaderboard_SearchPlaceholder"
        android:imeOptions="actionSearch"
        android:inputType="textPersonName"
        android:minHeight="48dp"
        android:singleLine="true" />

</TableLayout>